const strings = {
  Form: {
    fieldIsRequired: "Field is required",
    notValidEmail: "This is not a valid email address!",
    fieldNotMatch: "The field doesn't match with ",
    onlyNumbers: "Only numbers accepted!"
  }
}

export default strings;
